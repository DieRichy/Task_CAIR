# Task_CAIR

This repository reproduces the core uncertainty estimation experiments from  
**"Estimating LLM Uncertainty with Evidence" (Ma et al., 2025)**.

## 🧩 Environment Setup

This project uses [`uv`](https://github.com/astral-sh/uv) for environment management.

```bash
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt